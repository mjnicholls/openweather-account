/* eslint-disable */
const initialState = {
  user_id: '1',
  email: "user"
}

export default function authReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
